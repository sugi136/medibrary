# 2026-08-19 배포 검증 기록

- 대상 커밋: `77c0511` — `feat: paginate drug search and fix usage display`
- GitHub 원격 브랜치: `sugi136/medibrary`의 `main`
- Render 서비스: `medibrary-api` (`https://medibrary-api.onrender.com`)
- 확인 시각: 2026-08-19 09:52 GMT+9
- Render 자동 배포가 새 커밋으로 시작된 상태를 확인했다. 화면에는 이전 성공 배포 커밋 `8b62748`에서 새 커밋 `77c0511`으로 전환 중인 상태가 표시되었다.
- Render Free Web Service는 유휴 상태에서 중지될 수 있으며, 재기동 시 50초 이상 요청 지연이 발생할 수 있다는 운영 안내가 표시된다. 이 지연은 애플리케이션 검색 로직의 처리 시간이 아니라 무료 인스턴스 콜드 스타트에 따른 배포 환경 제약이다.
- 코드 차원에서는 결과 캐시와 조회 조건별 검색 메타데이터 캐시를 사용해, 인스턴스가 기동된 뒤 반복 검색이 외부 API를 다시 기다리지 않도록 개선했다.

Render 자동 배포가 약 3분 7초 후 성공 상태로 전환된 것을 확인했다. 운영 프런트엔드 `https://medibrary.vercel.app/search?shape=원형&color=하양`에서는 브라우저 탭 제목이 `메디브러리 | 의약품 정보 검색`으로 표시됐고, 모양·색깔 검색의 색상 선택값도 빈 값이 아니라 `하양`으로 정상 복원됐다. 검색 요청은 시작됐으며, Render Free 인스턴스가 유휴 상태였을 때는 외부 API 응답 전까지 로딩 상태가 유지될 수 있다.
